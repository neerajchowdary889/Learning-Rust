fn main(){
    // We are not suppose to use let keyword while writing constants.
    // we are defining constant just like final in java, we can't change variable in anywhere.
    const CON: u32 = 65;
    println!("{}",CON);
}